---
import { footerCards } from './FooterCardData';
---
<footer class="py-12 border-t border-gray-100 dark:border-gray-700 bg-white dark:bg-gray-900">
  <div class="max-w-6xl mx-auto px-4">
    <div class="flex flex-wrap justify-center gap-6">
      {footerCards.map(card => (
        <a href={card.link} target="_blank" rel="noopener" class="group flex-1 min-w-[200px] max-w-xs bg-gray-50 dark:bg-gray-800 rounded-xl shadow-md p-5 flex flex-col items-center transition-transform duration-200 hover:scale-105 hover:z-10 cursor-pointer">
          <img src={card.img} alt={card.title + ' logo'} class="w-16 h-16 object-contain mb-4 rounded" loading="lazy" />
          <h3 class="text-lg font-bold mb-1 text-gray-900 dark:text-white">{card.title}</h3>
          <p class="text-sm text-gray-600 dark:text-gray-300 text-center mb-2">{card.desc}</p>
        </a>
      ))}
    </div>
    <div class="mt-8 text-center text-xs text-gray-400">
      © {new Date().getFullYear()} Olcan E. Brem. Tüm hakları saklıdır.
    </div>
  </div>
</footer>
